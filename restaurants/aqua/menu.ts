import type { MenuItem } from '../../src/types';

const NOW = Date.now();

// CATEGORY IDS — confirm these from the menu PDF before adding items
// e.g. 'cold-meze', 'hot-meze', 'salads', 'grills', 'mains', 'sides', 'desserts', 'drinks'

// ITEM ID PREFIX SCHEME — pick one prefix per category, keep it consistent
// e.g. cm1 = cold meze, hm1 = hot meze, sa1 = salads, gr1 = grills ...

export const menuItems: MenuItem[] = [
  // items go here — add pairedItemIds from the start
];
